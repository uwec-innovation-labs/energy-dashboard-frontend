{
  "name": "energy",
  "version": "1.0.0",
  "description": "Downloaded .xlsx file, converted to .csv, deleted first two lines so first line was \"Timestamp, Value (BTU)\" (these values then become the column names)",
  "main": "read.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "connect": "^3.6.6",
    "express": "^4.16.4",
    "serve-static": "^1.13.2"
  }
}
